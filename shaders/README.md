# Shaders

Empty until Phase 4. This is where `glass.glsl` and `dotmatrix.glsl` land
(CLAUDE.md §5).

The dot-matrix uniform contract is already fixed in CLAUDE.md §5 — including the
Y-flip, since screen coords start top-left and shader UV starts bottom-left.
